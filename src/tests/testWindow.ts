/*import { render, screen } from "@testing-library/svelte";
import { expect, describe, test } from 'vitest';
import Window from "../window.svelte";

describe("Window", () => {
  const startPosition = {clientX: 100, clientY: 100};
  const StartWindowHeight = 400;
  const StartWindowWidth=200; 
  const isResizable = true;
  const getZindex = () => {return 1};

  test("shows the todo text when rendered", () => {
    render(Window, { props: { StartPosition: startPosition, StartWindowHeight:StartWindowHeight, StartWindowWidth:StartWindowWidth, isResizable:isResizable, getZindex:getZindex}});

    expect(screen.
    expect(screen.getByText(todoDone.text)).toBeInTheDocument();
  });
});*/